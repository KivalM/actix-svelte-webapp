//  This struct defines the user object that is used in the frontend homepage table
export interface User {
    email: string;
    username: string;
    name: string;
    password_hash: string;
    created_at: string;
}